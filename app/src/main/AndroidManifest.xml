<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.servicenovigrad">

    <uses-permission android:name="android.permission.INTERNET " />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_CALENDAR"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SuccursaleWorkHour"></activity>
        <activity android:name=".activities.ClientInformations" />
        <activity android:name=".activities.ClientRequest" />
        <activity android:name=".activities.ClientActivity" />
        <activity android:name=".activities.setHourActivity" />
        <activity android:name=".activities.SuccursaleAvailableServices" />
        <activity
            android:name=".activities.CreateSuccursale"
            android:label="@string/title_activity_create_succursale"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.EmployeeManageServices" />
        <activity android:name=".activities.employe_service" />
        <activity android:name=".activities.EmployeeOptions" />
        <activity android:name=".activities.SetServiceActivity" />
        <activity android:name=".activities.AdminActivity" />
        <activity
            android:name=".activities.DocumentUploadActivity"
            android:label="@string/title_activity_document_upload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.CreateServiceActivity" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:label="@string/title_activity_welcome_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.SignUpActivity"
            android:label="SignUpPage2" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>