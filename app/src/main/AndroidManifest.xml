<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.servicenovigrad">

    <uses-permission android:name="android.permission.INTERNET " />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".prototype_horaire"
            android:label="@string/title_activity_prototype_horaire"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".activities.EmployeeAddingService" />
        <activity android:name=".activities.EmployeeOptions" />
        <activity android:name=".activities.SetServiceActivity" />
        <activity android:name=".services.ListService" />
        <activity android:name=".activities.AdminActivity" />
        <activity
            android:name=".activities.DocumentUploadActivity"
            android:label="@string/title_activity_document_upload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.CreateServiceActivity" />
        <activity android:name=".AdminPage" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:label="@string/title_activity_welcome_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.SignUpActivity"
            android:label="SignUpPage2" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>